# The Git Bash shim must stay LF; the cmd shim must stay CRLF. The installer normalises both anyway.
scripts/claude       text eol=lf
scripts/claude.cmd   text eol=crlf
*.ps1                text eol=crlf
*.md                 text
*.json               text
